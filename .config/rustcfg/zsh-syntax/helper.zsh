#!/usr/bin/env zsh
# meant to be sourced
# https://zsh.sourceforge.io/Doc/Release/Variables-Index.html
zmodload zsh/parameter

# aliases -- aliases
# builtins -- built-in stuff like echo and print
# commands -- commands
# functions -- functions
# functions_source -- the sourcefiles for functions
# galiases -- global aliases
# reswords -- reserved words like if or for
# modules -- zsh modules
# nameddirs -- hashed directories `hash -d name=/path/dir`
# userdirs -- directories like ~vlk and ~sddm
# options -- zsh options
# KEYBOARD_HACK -- characters at the very end set to be ignored
# patchars -- enabled pattern characters
# saliases -- suffix aliases
# WORDCHARS -- chars that break words
# widgets -- zle functions, either user or non

# ${(@kv)parameters} -- the types of each variable


