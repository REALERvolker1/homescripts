# vim:ft=zsh
local files="$@"
local content="$(/usr/bin/cat "$@")"

if [ -z "$WAYLAND_DISPLAY" ]; then
    echo -n "$content" | /usr/bin/xclip -selection clipboard
else
    /usr/bin/wl-copy "$content"
fi

